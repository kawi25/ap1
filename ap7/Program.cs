using System;

namespace ap7
{
    class Program
    {
        static void Main(string[] args)
        {
            int num1, num2;

            Console.WriteLine("introduzca un numero");
            num1 = int.Parse(Console.ReadLine());
            Console.WriteLine("introduzca otro numero");
            num2 = int.Parse(Console.ReadLine());
            Console.WriteLine("la suma es" + suma(num1, num2));
            Console.WriteLine("la resta es" + resta(num1, num2));
            Console.WriteLine("la multiplicacion es" + multi(num1, num2())
            Console.WriteLine("la divicion es" + divi(num1, num2));

            }
              int static int  suma = (int num1, int num2)
            { 
              Variable 
                  suma = num1 , + num2 ;
             devolver suma;
            }
            static int resta ( int num1 , num2)
            {  
                variable 
                    resta num1, num2;
            }
            static int multi ( int num1 , int num2 )
            {
              doble numd1 = convertir.paraDoble(num1);
               doble numd2 = convertir.paradoble(num2);
        }
              volver numd1 / numd2;

    }
}
